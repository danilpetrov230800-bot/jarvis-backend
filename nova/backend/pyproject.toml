[project]
name = "nova-backend"
version = "1.0.0"
description = "NOVA - Neural Operational & Virtual Assistant Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["../../tests/unit", "../../tests/integration"]
