# NOVA 1.5.0

Windows release package includes `NOVA-Setup.exe`, `NOVA-Portable.zip` and `SHA256.txt` after the Windows packaging workflow completes.

This release moves NOVA user data to the current Windows profile, protects API keys with DPAPI, and adds durable local services for memory, skills, tasks, permissions, diagnostics and profile backup.
